# games/trivia/

## 진실의 원천
- 모든 답변 도착 시각은 **서버 도착 시각만 사용**. 클라가 보낸 timestamp 신뢰 안 함. payload는 `{ qIndex, choice }`만.
- 문제당 첫 답만 기록. 같은 `qIndex`에 두 번째 답 오면 무시.
- ranking은 `computeResult` 안에서만 결정. Renderer는 받은 `schedule`/`questions`만 보고 페이즈 분기.

## 결정성
- `buildQuizPlan(seed, sortedPool)` — mulberry32 단일 스트림으로 (1) 풀에서 N문제 비복원 추출(형제 문항 배제 + 난이도 쿼터 + 카테고리별 라운드 점유율 ~60% 상한, 풀이 못 채우면 중요도 역순으로 제약을 풀며 충원), (2) 각 문제 보기 4개 셔플. 같은 seed·같은 풀이면 항상 같은 문제·같은 보기 순서.

## 난이도 쿼터
문항마다 `difficulty` 1(쉬움)/2(보통)/3(어려움) **필수** — 라운드는 항상 쉬움 2·보통 2·어려움 1로 뽑히고 쉬움→어려움 순으로 정렬된다(마지막 문제가 2배 배점이라 제일 어려운 게 피날레로 간다). 태그가 하나도 없는 풀(넌센스)은 쿼터를 아예 끈다 — 안 그러면 전부 "보통"으로 읽혀 쿼터가 2에서 막히고, 대신 카테고리 상한이 풀려버린다.

어려움 슬롯은 라운드당 1개뿐이라 **어려움 문항 수가 곧 셔플백 주기다** — 54개면 54라운드마다 어려움 티어가 한 바퀴 돈다. 신선도 갈증이 가장 먼저 오는 곳이 여기라, 반복이 눈에 띄면 어려움 문항을 늘리는 쪽이 먼저다.

난이도는 **문항을 쓴 사람이 스스로 매기면 거의 항상 과대평가된다** — 실제로 어려움으로 제출된 40문항 중 14개가 검증에서 보통·쉬움으로 강등됐다(금성이 수성보다 뜨겁다, 물의 최대 밀도 4°C, 법랑질이 가장 단단하다 등은 한국 교과·대중 트리비아로 이미 닳은 소재). 새 문항의 난이도는 작성자와 다른 눈으로 한 번 더 판정할 것.

## 형제 문항 배제
같은 템플릿에 정답 후보가 닫힌 집합인 문항끼리는 한 라운드에 못 나온다(베토벤 별명 3문항, 영화제 최고상 3문항, 세종 과학기구 4문항 등). 앞 문항 정답 공개가 뒷 문항의 보기를 소거해 난이도가 무너지기 때문. 계보는 [questions.ts](questions.ts)의 `EXCLUSIVE_GROUPS` 한 곳에만 — 문항 리터럴엔 절대 안 적는다(`TRIVIA_POOL_SORTED`가 로드 시 붙여줌). 한 문항이 여러 묶음에 속해도 된다.

숫자만 우연히 겹치는 무관한 문항(행성 8개 ↔ 거미 다리 8개)은 넣지 말 것 — 서로 정보를 주지 않는데 추출만 좁아진다.
- 풀(`TRIVIA_POOL`)은 `id` 사전순(`TRIVIA_POOL_SORTED`)으로 정렬한 뒤 추출. 풀 끝에 새 문제를 추가해도 기존 id의 추출 매핑은 안 깨짐.
- 동률 tie-break: 점수 DESC → 정답 응답속도 합 ASC → seed 파생 셔플 순서(`seededTieRank`). 토큰 사전순은 방 생성 후 고정이라 manual 2명 이상이면 매판 같은 사람이 지게 되어 폐기.

## 방별 출제 이력 (반복 방지)
`buildQuizPlan(seed, pool, excludeIds)`의 `excludeIds`는 그 방이 **이미 받은 문항 id**다. 파티는 한 자리에서 여러 판을 돌리는데, 345문항 풀도 20라운드면 순수 확률로 슬롯의 ~13%가 겹친다("아까 그거 또 나왔네").

**타이밍이 핵심이다.** `rounds/quiz.ts`가 라운드 시작에 목록을 스냅샷으로 떠서 `buildQuizPlan`에 넘기고, **같은 스냅샷**을 `computeResult`까지 들고 간다(`ComputeResultInput.excludeIds`). 결과 시점에 `room.servedQuestions`를 다시 읽으면 안 된다 — 그때는 이번 라운드 문항이 이미 누적돼 있어서 **다른 계획이 만들어지고, 플레이한 문항이 아닌 것들로 채점된다.** 누적은 라운드가 결과를 낸 뒤에만 한다(중간에 죽은 라운드는 문항을 소모하지 않는다).

가방이 비면 이력을 지워 새 사이클을 시작한다(`refreshServedBag`, server.ts) — **단, 재는 건 전체 풀이 아니라 난이도 티어별 잔량이다.** 추출이 티어별이라 먼저 마르는 티어가 곧 병목이고, 전체 잔량만 보면 그 병목을 못 본다: 어려움 54문항 × 라운드당 1개면 54라운드에 마르는데 363문항 기준 전체 검사는 71라운드까지 안 걸려서, 그 사이 17판이 이미 낸 어려움 문항을 다시 냈다(픽커는 신선도를 쿼터보다 **먼저** 포기하도록 설계돼 있다 — 그건 의도고, 그 상태에 도달하지 않게 막는 게 이 함수 몫이다).

그래서 리프레시는 전부-아니면-전무가 아니라 **소진된 티어의 id만** 떨어낸다. 나머지 티어는 남은 신선도를 그대로 유지한다. 난이도 태그가 없는 풀(넌센스)은 쿼터가 꺼져 있으니 전체 잔량 검사 하나로 축퇴 — 기존 동작 그대로다.

회귀 방지는 `quiz-pools.test.ts`의 **200라운드** 파티 테스트다. 20라운드짜리로는 절대 못 잡는다 — 병목 티어에 닿기도 전에 끝난다.

## 페이즈
- 문제별 `[openAt, closeAt)` 윈도우 안에서만 답 수락. 그 외 무시.
- 클라이언트 문제 노출/정답 공개는 `replay.data.schedule`(서버 권위) 기반 wall-clock으로 자동 분기.
- **정답 인덱스는 사전 배포 금지** — `game:start`/mid-play state의 문항은 answer-free(`TriviaIntroData`), 각 문항 정답은 closeAt의 `trivia:standings.correctIndex`로만 push. `game:start`의 seed도 마스킹(풀이 공개라 seed만으로 계획 전체가 역산됨).
- 단축(short-circuit)으로 스케줄이 당겨지면 `trivia:reschedule` 브로드캐스트뿐 아니라 state에 실리는 intro의 `schedule`도 같이 갱신한다 — 라운드 중 새로고침한 클라는 그 intro로 `game:start`를 재구성하므로, 안 맞추면 남은 라운드 내내 엉뚱한 문항을 본다.

## 금기
- `Math.random()` / `Date.now()`를 `computeResult`/`buildQuizPlan`에서 호출 금지.
- `computeResult` 안에서 방 상태(출제 이력 등)를 **직접 읽기 금지**. 필요한 건 전부 `ComputeResultInput`으로 받는다 — 시작 시점과 결과 시점 사이에 방 상태가 변하면 계획이 갈라진다.
- 클라가 보낸 timestamp를 ranking에 직접 반영 금지.
- `trivia:answer` payload에 timestamp 추가 금지 (스푸핑 방지).
- Renderer에서 정답 재계산 / 점수 재계산을 ranking에 사용 금지. 서버 ranking이 진실.

## 문제 풀 추가
1. [questions.ts](questions.ts)의 `TRIVIA_POOL`에 새 항목 append.
2. `id`는 kebab-case, 다른 항목과 안 겹치게.
3. 가볍고 무난한 톤 유지: 정치·종교·민감 주제 제외. 사내/회사·시사성 정보는 노화 위험 → 제외.
4. `question` ~40자, `choices` 각 ~20자(모바일 한 줄), `difficulty` 1~3 필수(빼면 컴파일 에러).
5. 기존 문항과 정답이 겹치거나 보기 세트가 거의 같으면 `EXCLUSIVE_GROUPS`에 묶는다. `npx vitest run src/games/quiz-pools.test.ts`가 정답 중복·계보 오타를 잡아준다.
6. `note`는 사실만 — 통설·일화는 "~라는 설"로 표기하고, 시간이 지나면 거짓이 될 문장("사상 처음이자 마지막")은 쓰지 않는다.
7. **오답 3개가 각각 거짓인지 개별 확인**. 정답만 검증하면 놓친다 — "우주비행사의 몸 변화" 문항은 오답 '체온이 올라간다'가 실제로 참(ISS 심부체온 상승)이었고, 한니발 문항은 오답 '노새'가 폴리비오스 기록상 참이었다. 둘 다 정답이 두 개인 문항이었다.
